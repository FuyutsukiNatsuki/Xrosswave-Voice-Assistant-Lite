# Dev/build-only dependencies (not needed to run from source).
-r requirements.txt

# Packaging the app into a standalone executable.
pyinstaller

# Generating the app icon (scripts/make_icon.py).
pillow
